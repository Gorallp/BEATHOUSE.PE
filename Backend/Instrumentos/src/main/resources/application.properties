spring.application.name=com.beathouse

# Datasource configuration read from environment to avoid committing credentials
# Expect an environment variable named `DATABASE_URL_JDBC` with a full JDBC URL,
# e.g. jdbc:mysql://user:pass@host:port/database4

spring.datasource.url=jdbc:mysql://31.97.248.239:4778/default?useSSL=false&serverTimezone=UTC&user=mysql&password=fvMH03Sp9HbGoh63Sp9MJuCrDvAYh8c8gpCVdhEZxqEYPowFqAZr5EzFGCTkvidb

# Do NOT modify DB structure from the app. Use 'none' or 'validate' depending on
# whether you want Hibernate to only validate mappings against schema.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
server.port=8081
# Recommended MySQL properties
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Directory where uploaded images will be stored (relative to working dir)
app.upload.dir=uploads
